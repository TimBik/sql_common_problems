SELECT ename, job, sal
FROM emp
WHERE deptno = 10
ORDER BY sal;

SELECT ename, job, sal
FROM emp
WHERE deptno = 10
ORDER BY sal desc;

SELECT ename, job, sal
FROM emp
WHERE deptno = 10
ORDER BY 3 asc;
